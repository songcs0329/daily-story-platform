# 설계 노트

매일 AI가 단편소설과 썸네일을 만들어 올리고, 사용자는 읽고 댓글을 남기는 서비스.
구조와 흐름은 [FLOWCHART.md](./FLOWCHART.md), 테이블은 [apps/api/ERD.md](./apps/api/ERD.md).

이 문서는 "무엇을 만들었나"가 아니라 **"왜 그렇게 했나"**를 남긴다.

## 제약 조건

설계를 지배한 건 기능 요구사항이 아니라 이 세 가지였다.

- **1인 개발, 운영 인력 없음** — 새벽에 깨워서 고쳐야 하는 구조는 만들 수 없다
- **무료 티어** — Gemini는 모델별 일일 쿼터가 20~500건, 429/503이 예외가 아니라 일상
- **매일 정해진 시각에 반드시 돌아야 함** — 사람이 개입하지 않는 파이프라인

## 결정

### 1. 생성 파이프라인을 API 서버 밖으로 뺐다

일일 생성은 NestJS의 `@Cron`이 아니라 **Google Apps Script**(`scripts/apps-script/daily-story-generator.gs`)가 담당한다. Gemini 호출 → Supabase Storage 업로드 → `posts` insert까지 전부 직접 처리하므로, `apps/api`가 잠들어 있어도 생성은 돈다.

- 서버 안에 크론을 두면 **글 생성이 웹 서버의 가동 시간에 종속**된다. 읽기 트래픽이 없는 새벽에 서버가 자고 있으면 그날 글이 없다.
- Apps Script는 스케줄러 + 실행 환경 + 시크릿 저장소(Script Properties) + 실패 알림을 전부 무료로 제공한다. 워커나 별도 스케줄러 인프라를 추가하지 않았다.
- 대안이던 GitHub Actions cron은 시크릿·재시도 관리 부담이 비슷한데 이점이 없어 기각.
- **대가**: 코드가 리포와 Apps Script 콘솔 두 곳에 존재하고 수동 동기화가 필요하다. 배포 빈도가 낮아 감수했다. 잦아지면 clasp을 붙인다.

`apps/api/src/generation/`은 같은 일을 하는 수동 테스트용 엔드포인트로 남겨뒀다 — 스케줄에는 연결돼 있지 않다.

### 2. 실패를 알림 인프라 없이 알린다

생성 실패 지점(장르 조회 / 텍스트 / 이미지)은 로그만 남기고 조용히 종료하는 대신 **예외를 던진다**. Apps Script가 트리거 예외에 대해 스크립트 소유자에게 실패 메일을 자동 발송하기 때문이다.

`MailApp.sendEmail`을 직접 호출하는 방법도 있었지만, 플랫폼이 이미 하는 일을 코드로 옮기면 OAuth 스코프만 하나 늘어난다. 모니터링 서비스는 하루 두 건 도는 작업에 과하다.

### 3. 모델을 하나로 고정하지 않았다

텍스트·이미지 각각 **폴백 체인**(`GEMINI_MODELS`, `GEMINI_IMAGE_MODELS`)을 두고 위에서부터 시도, 429/503/404면 즉시 다음 모델로 넘어간다.

무료 쿼터 환경에서 단일 모델 고정은 "쿼터 소진 = 그날 글 없음"이다. 지수 백오프 재시도는 쿼터가 리셋될 때까지 기다리는 것이라 의미가 없고, 다른 모델로 갈아타는 게 맞는 대응이다. 배열 순회 한 번으로 끝나서 재시도 라이브러리도 큐도 넣지 않았다.

체인 순서는 쿼터가 넉넉한 라이트 모델 우선, 마지막에 안전망 모델. 실계정 검증 결과를 코드 주석에 남겼다.

### 4. 장르와 프롬프트를 DB에 뒀다

`genres` 테이블이 `slug` / `label` / `story_prompt`를 갖고, `posts.genre`가 `genres.slug`를 FK로 참조한다. 생성 스크립트는 매 실행마다 여기서 장르를 랜덤으로 고른다.

핵심은 확장성이 아니라 **운영 편의**다. 프롬프트가 DB에 있으면 문장을 다듬을 때 코드 수정·배포가 필요 없다. 장르 추가도 `insert` 한 줄.

`posts.genre`를 Postgres enum이 아니라 `text` + FK로 한 것도 같은 이유 — enum 값 추가는 스키마 마이그레이션이지만 FK는 데이터 한 행이다. (초기엔 enum이었고, `supabase/schema.sql` 하단에 전환 마이그레이션이 남아 있다.)

### 5. 로그인 없이도 읽을 수 있다

가드는 두 개뿐이다 (`apps/api/src/auth/auth.guard.ts`).

- `AuthGuard` — 실패 시 401. 댓글 작성·삭제
- `OptionalAuthGuard` — 토큰이 있으면 사용자를 주입하고, 없어도 통과. 글 상세

읽기를 막으면 유입이 죽는다. 그래서 상세는 누구나 보되, **조회수는 로그인 사용자의 요청에만 증가**시킨다(`posts.service.ts`). 봇과 익명 새로고침 노이즈를 조회 로그 테이블 없이 한 줄로 걸러낸 것이다.

인증은 카카오 OAuth로 신원을 받고 자체 JWT를 발급하는 것까지만 — 세션 저장소도, 리프레시 토큰도 없다.

### 6. 프론트 상태를 두 축으로 나눴다

- **서버 데이터** → TanStack Query 전부. 목록은 `useInfiniteQuery` + IntersectionObserver
- **클라이언트 상태** → Zustand는 인증 하나뿐 (`useAuthStore`, persist로 새로고침 유지)

뮤테이션 성공 시 캐시를 직접 수정하지 않고 `invalidateQueries`로 재조회한다. 댓글 응답은 서버가 작성자 정보를 조립해서 내려주는데, 그 조립 로직을 프론트에 복제하면 두 곳이 어긋난다.

인증 토큰을 만지는 코드는 `restClient.ts` 한 파일에 갇혀 있다 — 요청 인터셉터가 헤더를 붙이고, 응답 인터셉터가 401을 받으면 로그아웃시킨다. 컴포넌트는 토큰의 존재를 모른다.

### 7. Supabase를 DB로만 쓴다

`apps/api`는 PostgREST(anon/authenticated 롤)가 아니라 `DATABASE_URL`로 Postgres에 직접 붙는다. 따라서 **RLS 정책의 적용 대상이 아니고**, 권한 판단은 전부 API 레이어에 있다.

BaaS를 프론트에 직접 물리면 권한 로직이 RLS 정책과 서버 코드로 쪼개진다. 한 곳에 모으는 쪽을 택했다. Supabase의 REST를 쓰는 건 Apps Script뿐이고, 거기서만 service key를 쓴다.

## 의도적으로 넣지 않은 것

| 항목 | 이유 |
|---|---|
| 좋아요 / 검색 / 장르 필터 | 하루 2편, 장르 6종. 필터할 모수가 없다 |
| 댓글 페이지네이션 | 한 글의 댓글이 많아지면. 코드에 `ponytail:` 주석으로 유예 표시 |
| 조회수 어뷰징 방지 | 정확한 집계엔 조회 로그 테이블이 필요. 수치가 의사결정에 쓰이기 전엔 낭비 |
| 리프레시 토큰 | 만료가 실제로 성가셔지면. 지금은 401 → 로그아웃 |
| 글별 동적 og:image | SPA라 프리렌더가 필요하다. 사이트 공통 태그까지만 |

## 알려진 부채

- **TypeORM `synchronize`** — 비프로덕션 한정으로 켜져 있다. 실데이터가 쌓이기 전에 마이그레이션으로 전환해야 한다
- **Apps Script 수동 동기화** — 리포를 고쳐도 실제 스크립트는 안 바뀐다. 붙여넣기를 잊으면 조용히 옛 코드가 돈다
- **Supabase 키 정책** — Apps Script는 레거시 JWT `service_role` 키를 쓴다. 새 `sb_secret_` 키는 `UrlFetchApp`을 브라우저로 오인해 401로 막았고, User-Agent 헤더 우회도 실패해 레거시 키로 되돌렸다. 레거시 키는 2026년 말 폐기 예정이라 그 전에 대안이 필요하다 (`.gs`의 `SUPABASE_USER_AGENT`는 그때의 출발점으로 남겨둔 잔재)

## 스코프가 어떻게 변했나

초기 스코프는 "읽기 전용 게시판, 장르는 유니언 타입으로 하드코딩"이었고, 인증·댓글은 명시적으로 제외였다. 지금은 셋 다 들어와 있다.

되짚어보면 **읽기 전용 서비스는 재방문 이유가 없었다.** 댓글이 들어오면서 사용자를 식별할 필요가 생겼고, 그게 인증을 끌고 왔다. 장르도 2종 하드코딩에서 6종 테이블로 옮겼는데, 확장성 때문이 아니라 프롬프트를 배포 없이 고치고 싶어서였다.

즉 확장 판단의 기준은 "언젠가 필요할까"가 아니라 **"지금 이걸 안 하면 무엇이 막히나"**였다. 그 기준은 `CLAUDE.md`에도 규칙으로 옮겨 적었다.
